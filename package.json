{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"npm run build\" && hugo"
  },
  "dependencies": {
    "source-map-resolve": "^0.6.0",
    "source-map-url": "^0.4.1",
    "urix": "^0.1.0",
    "express": "^4.17.1",
    "netlify-cli": "^12.7.2",
    "netlify-lambda": "^2.0.15",
    "serverless-http": "^2.7.0"
  }
}
